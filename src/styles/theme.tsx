interface Theme {
  bg: {
    common: string;
    button: string;
    border: string;
    boldBorder: string;
  };
}

const theme: Theme = {
  bg: {
    common: "#FFFFFF",
    button: "#DD1463",
    border: "#DDDDDD",
    boldBorder: "#b0b0b0",
  },
};

export default theme;
